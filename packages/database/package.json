{
  "name": "@triae/database",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "generate": "pnpm run load:env prisma generate",
    "migrate": "pnpm run load:env prisma migrate dev",
    "deploy": "pnpm run load:env prisma migrate deploy",
    "reset": "pnpm run load:env prisma migrate reset",
    "load:env": "dotenv -e ../../.env --"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "5.9.2"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.0.1",
    "@prisma/client": "^7.0.1",
    "@triae/env": "workspace:^",
    "dotenv": "^17.2.3",
    "dotenv-cli": "^11.0.0",
    "prisma": "^7.0.1"
  }
}